POSTGRES_USER=researcher_admin
POSTGRES_PASSWORD=change-me-before-deploying
POSTGRES_DB=warwalking_fitness
DATABASE_URL=postgres://researcher_admin:change-me-before-deploying@warwalking-db:5432/warwalking_fitness

# Fernet key used to encrypt WiGLE API tokens at rest.
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
TOKEN_ENCRYPTION_KEY=replace-with-generated-fernet-key
